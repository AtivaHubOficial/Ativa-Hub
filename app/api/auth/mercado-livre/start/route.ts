import { NextRequest,NextResponse } from "next/server";
import { requireAdmin } from "@/lib/admin-authorization";
import { createAuthorizationRequest, ML_STATE_COOKIE, MercadoLivreOAuthError, secureCookieOptions } from "@/lib/mercado-livre-oauth";
import {resolveMercadoLivreOAuthContext} from "@/lib/mercado-livre-oauth-context";
export const runtime="nodejs";
export async function GET(request:NextRequest){const context=resolveMercadoLivreOAuthContext(request.url,process.env.MERCADO_LIVRE_REDIRECT_URI);if(context.delegateToRegisteredOrigin)return NextResponse.redirect(new URL("/api/auth/mercado-livre/start",context.origin));const admin=await requireAdmin();if(!admin.ok)return NextResponse.redirect(new URL(admin.status===401?"/admin/login":"/admin/acesso-negado",context.origin));try{const authorization=createAuthorizationRequest(context.redirectUri);const response=NextResponse.redirect(authorization.url);response.cookies.set(ML_STATE_COOKIE,authorization.stateCookie,secureCookieOptions(10*60,context.secureCookie));return response}catch(error){console.error("[ml-oauth] start failed",{code:error instanceof MercadoLivreOAuthError?error.code:"INTERNAL_ERROR",message:error instanceof Error?error.message:String(error)});return NextResponse.json({error:"Não foi possível iniciar a autorização do Mercado Livre."},{status:error instanceof MercadoLivreOAuthError?error.status:500})}}
