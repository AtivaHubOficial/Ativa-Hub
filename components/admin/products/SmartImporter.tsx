"use client";
import { useState } from "react";
import { Link2,Sparkles } from "lucide-react";
import type { ImportedProduct } from "@/types/product-import";
type ImportResponse={product?:ImportedProduct;error?:string;code?:string;requestId?:string;authorizeUrl?:string};
export default function SmartImporter({onImported}:{onImported:(product:ImportedProduct)=>void}){const[url,setUrl]=useState("");const[pending,setPending]=useState(false);const[message,setMessage]=useState("");const[authorizeUrl,setAuthorizeUrl]=useState("");async function run(){setPending(true);setMessage("");setAuthorizeUrl("");try{const response=await fetch("/api/admin/import-product",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url}),signal:AbortSignal.timeout(20_000)});const contentType=response.headers.get("content-type")??"";if(!contentType.includes("json"))throw new Error(`O endpoint interno retornou uma resposta inesperada (${response.status}).`);const body=await response.json() as ImportResponse;if(!response.ok||!body.product){if(body.authorizeUrl)setAuthorizeUrl(body.authorizeUrl);const diagnostic=body.requestId?` Código de diagnóstico: ${body.requestId}.`:"";throw new Error(`${body.error??`O endpoint interno falhou (${response.status}).`}${diagnostic}`)}onImported(body.product);setMessage(body.product.warning??"Dados importados. Revise os campos e as imagens antes de cadastrar.")}catch(error){if(error instanceof Error&&(error.name==="TimeoutError"||error.name==="AbortError"))setMessage("O endpoint interno excedeu 20 segundos. Tente novamente.");else if(error instanceof TypeError)setMessage("Erro de rede ao chamar o endpoint interno de importação.");else setMessage(error instanceof Error?error.message:"Falha inesperada no endpoint interno.")}finally{setPending(false)}}return <section className="rounded-2xl border border-amber-200 bg-amber-50 p-5 shadow-sm sm:p-6"><div className="flex items-start gap-3"><span className="rounded-xl bg-amber-400 p-2"><Sparkles size={20}/></span><div><h2 className="text-lg font-black">Importador inteligente</h2><p className="mt-1 text-sm text-slate-600">Cole um anúncio do Mercado Livre. Nada será salvo antes da sua revisão.</p></div></div><div className="mt-4 flex flex-col gap-2 sm:flex-row"><label className="relative flex-1"><span className="sr-only">URL do produto</span><Link2 className="absolute left-3 top-3.5 text-slate-400" size={18}/><input type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://produto.mercadolivre.com.br/MLB-..." className="min-h-12 w-full rounded-xl border border-amber-200 bg-white pl-10 pr-3 text-sm outline-none focus:border-amber-500"/></label><button type="button" onClick={run} disabled={pending||!url.trim()} className="min-h-12 rounded-xl bg-slate-900 px-5 font-bold text-white disabled:opacity-50">{pending?"Importando…":"Importar dados"}</button></div>{message?<p role="status" className="mt-3 whitespace-pre-line text-sm font-bold text-slate-700">{message}</p>:null}{authorizeUrl?<a href={authorizeUrl} className="mt-3 inline-flex min-h-11 items-center rounded-xl bg-amber-400 px-4 text-sm font-black text-slate-950">Conectar Mercado Livre</a>:null}</section>}
